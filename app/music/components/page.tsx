"use client";

// This is an intentionally empty component file
// It exists only to satisfy Next.js's file routing requirements
// The actual content will be loaded dynamically via the root page

export default function EmptyComponent() {
  return null;
}